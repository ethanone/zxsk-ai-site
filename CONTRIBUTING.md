# 🤝 协作开发指南

本文档说明如何作为协作开发者参与项目开发。

---

## 🚀 快速开始

### 1. Fork 或 Clone 仓库

```bash
# 克隆仓库
git clone https://github.com/ethanone/zxsk-ai-site.git
cd zxsk-ai-site

# 安装依赖
npm install
```

### 2. 创建功能分支

**⚠️ 重要：永远不要直接修改 `main` 分支！**

```bash
# 确保 main 分支是最新的
git checkout main
git pull origin main

# 创建新分支（使用描述性的名称）
git checkout -b feature/your-feature-name

# 或者修复 bug
git checkout -b fix/bug-description
```

**分支命名规范：**
- `feature/xxx` - 新功能
- `fix/xxx` - Bug 修复
- `refactor/xxx` - 代码重构
- `docs/xxx` - 文档更新

### 3. 开发并提交

```bash
# 修改代码...

# 提交更改
git add .
git commit -m "feat: 添加新功能描述"

# 提交信息格式（推荐）：
# feat: 新功能
# fix: 修复bug
# docs: 文档更新
# style: 代码格式（不影响功能）
# refactor: 重构
# test: 测试
# chore: 构建/工具变动
```

### 4. 推送到 GitHub

```bash
# 推送您的分支（不是 main！）
git push origin feature/your-feature-name
```

### 5. 创建 Pull Request

1. 访问 GitHub 仓库页面
2. 点击 **"Pull requests"** → **"New pull request"**
3. 选择：
   - **base**: `main` (目标分支)
   - **compare**: `feature/your-feature-name` (您的分支)
4. 填写 PR 描述：

```markdown
## 📝 修改内容
- [ ] 修改点 1
- [ ] 修改点 2

## 🧪 测试
- [ ] 本地测试通过
- [ ] 移动端测试通过
- [ ] 桌面端测试通过

## 📸 截图（如有 UI 改动）
[上传截图]

## 🔗 相关 Issue
Closes #123
```

5. 点击 **"Create pull request"**
6. @主账号 进行 review

---

## ✅ PR 审查流程

### 自动检查

创建 PR 后，Vercel 会自动：
- ✅ 生成预览链接（如：`xxx-pr-123.vercel.app`）
- ✅ 运行构建检查
- ✅ 显示部署状态

### 审查清单

在合并前，请确认：

- [ ] 代码符合项目规范
- [ ] 没有控制台错误
- [ ] 响应式布局正常（手机/平板/桌面）
- [ ] 中英文内容都已更新
- [ ] 无拼写错误
- [ ] 预览链接测试通过

### 主账号合并

审查通过后，主账号会：
1. 点击 **"Merge pull request"**
2. 选择合并方式（推荐 "Squash and merge"）
3. 确认合并
4. 代码自动部署到生产环境

---

## 🔄 更新您的分支

如果 `main` 分支有更新，需要同步到您的分支：

```bash
# 切换到您的分支
git checkout feature/your-feature-name

# 拉取最新的 main
git fetch origin
git merge origin/main

# 或者使用 rebase（更推荐）
git rebase origin/main

# 解决冲突（如有）
# ... 编辑冲突文件 ...

# 继续 rebase
git add .
git rebase --continue

# 强制推送（因为历史已改变）
git push origin feature/your-feature-name --force-with-lease
```

---

## ⚠️ 重要规则

### ❌ 禁止操作

- ❌ **不要直接 push 到 `main` 分支**
- ❌ **不要强制推送 `main` 分支**
- ❌ **不要在 `main` 分支上直接开发**

### ✅ 推荐做法

- ✅ **始终使用功能分支**
- ✅ **每个 PR 只做一件事**
- ✅ **提交信息清晰明确**
- ✅ **测试通过后再创建 PR**
- ✅ **及时同步 `main` 分支**

---

## 🐛 遇到问题？

### 问题 1: 推送被拒绝

```
error: failed to push some refs to 'origin/main'
```

**原因**：尝试直接推送到 `main` 分支

**解决**：使用功能分支，创建 PR

### 问题 2: 分支已过时

```
error: Updates were rejected because the remote contains work
```

**解决**：
```bash
git pull origin main --rebase
# 解决冲突后
git push origin feature/your-branch
```

### 问题 3: 权限不足

```
remote: Permission denied
```

**解决**：确认您有仓库的 Write 权限，或联系主账号

---

## 📚 参考资源

- [Git 工作流最佳实践](https://www.atlassian.com/git/tutorials/comparing-workflows)
- [GitHub Flow](https://guides.github.com/introduction/flow/)
- [语义化提交信息](https://www.conventionalcommits.org/)

---

## 💡 提示

1. **小步提交**：频繁提交，每次提交一个完整的功能点
2. **描述清晰**：PR 描述要详细，方便审查
3. **及时沟通**：有问题及时在 PR 中讨论
4. **保持同步**：定期拉取 `main` 分支更新

---

<div align="center">

**Happy Coding! 🚀**

如有疑问，请在 PR 中 @主账号 或创建 Issue

</div>






