# ⚡ 协作工作流快速指南

## 🎯 当前情况

- ✅ 您是**协作账号**，有 Write 权限
- ✅ 项目已配置 Vercel 自动部署
- ⚠️ **不要直接 push 到 main**（需要主账号设置分支保护）

---

## 🚀 标准工作流程（3步）

### Step 1: 创建分支并开发

```bash
# 1. 确保 main 是最新的
git checkout main
git pull origin main

# 2. 创建功能分支
git checkout -b feature/your-feature-name

# 3. 开发并提交
git add .
git commit -m "feat: 您的修改描述"
```

### Step 2: 推送到 GitHub

```bash
# 推送您的分支（不是 main！）
git push origin feature/your-feature-name
```

### Step 3: 创建 Pull Request

1. 访问：`https://github.com/ethanone/zxsk-ai-site/pulls`
2. 点击 **"New pull request"**
3. 选择：
   - base: `main`
   - compare: `feature/your-feature-name`
4. 填写描述，点击 **"Create pull request"**
5. @主账号 进行 review

---

## ✅ 之后会发生什么

1. **Vercel 自动生成预览链接**（在 PR 页面可见）
2. **主账号审查代码**
3. **测试预览链接**
4. **主账号合并 PR**
5. **Vercel 自动部署到生产环境**（1-5分钟）

---

## 📋 当前待推送的代码

如果您有未推送的代码，按以下方式处理：

```bash
# 1. 查看当前状态
git status

# 2. 如果有未提交的更改
git add .
git commit -m "feat: 您的修改描述"

# 3. 创建功能分支（如果还在 main 上）
git checkout -b feature/template-refactor

# 4. 推送分支
git push origin feature/template-refactor

# 5. 在 GitHub 创建 PR
```

---

## 🔧 如果主账号还没设置分支保护

**临时方案**（仅限紧急情况）：

```bash
# 直接推送（不推荐，但可用）
git push origin main
```

**但建议**：
- 联系主账号设置分支保护
- 养成使用 PR 的习惯
- 即使没有保护，也使用分支 + PR 流程

---

## 💡 最佳实践

### ✅ 推荐

```bash
# 每次修改都创建新分支
git checkout -b feature/xxx
git commit -m "feat: xxx"
git push origin feature/xxx
# 然后创建 PR
```

### ❌ 避免

```bash
# 不要这样做
git checkout main
git commit -m "fix: xxx"
git push origin main  # ❌ 直接推 main
```

---

## 🆘 紧急情况

如果必须立即上线：

1. 创建分支并推送
2. 创建 PR
3. 在 PR 中 @主账号 并说明紧急原因
4. 主账号可以快速合并

---

## 📞 需要帮助？

- 查看详细文档：[CONTRIBUTING.md](./CONTRIBUTING.md)
- 在 PR 中 @主账号
- 创建 Issue 提问

---

<div align="center">

**记住：分支 + PR = 安全可控 🛡️**

</div>






